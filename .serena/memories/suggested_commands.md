Primary development commands:

- mise install
- pnpm install
- mise run build
- mise run test
- mise run check
- mise run format
- mise run cli
- TARGET=x86_64-unknown-linux-musl mise run cli-target
- pnpm --filter @taiga-tech/cli run build
- node packages/cli/dist/index.cjs
- scripts/place-binary-like-ci.sh --build
  Useful system commands on Darwin: git status, git diff, rg, rg --files, find, ls, sed -n, pnpm, cargo.
